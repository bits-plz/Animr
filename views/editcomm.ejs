<%- include('partials/header') %>
<div class="ui main text container segment">
    <div class="ui huge header">COMMENT</div>
    <form class="ui form " action="/anim/<%= anim_id %>/comments/<%= comment._id %>?_method=PUT" method="POST">
        <div class="field"><label>Comment</label><textarea name='comment[text]' ><%=comment.text  %></textarea></div>
        <div class="field"><button type="submit" class="ui purple basic button">SUBMIT</button></div>
    </form>
</div>
<%- include('partials/footer')%>